<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    

    <title>Note app</title>

</head>
<body>
    <div class="container">
        <div style="width: 50%; margin: 10% auto 0">
            <a href="/notes/create" class="btn btn-success">Tạo note</a>
        </div>
        <div class="card" style="width: 50%; margin: 10px auto">
            <div class="card-header text-center text-white bg-primary">Danh sách ghi chú</div>
            <ul class="list-group">
                <% if (notes.length <= 0) { %>
                    <li class="list-group-item">
                        <p class="text-center">Không có note để hiển thị</p>
                    </li>
                <% } else { notes.forEach(note =>{ %>
                    <li class="list-group-item d-flex">
                        <a href="#box-data-<%=note._id %>"
                            class="text-decoration-none "  
                            data-bs-toggle = "collapse" data-bs-target="#box-data-<%=note._id %>"
                            id="click-load-data"
                            style="color: black; flex-basis: 90%;">
                            <%= note.head %> 
                        </a>
                        <form action="/notes/<%= note._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-danger ">Xóa</button>
                        </form>
                        
                    </li>
                    <div id="box-data-<%=note._id %>" class="collapse p-3">
                        <p id="data"><%= note.content %> </p>
                        <button class="btn btn-secondary"
                        data-bs-toggle = "collapse" data-bs-target="#box-data-<%=note._id %>"
                        >Thu</button>
                        <a href="/notes/<%=note._id %>/edit" class="btn btn-info">Sửa</a>
                    </div>
                <% }) }%>
            </ul>
        </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js" 
    integrity="sha384-SR1sx49pcuLnqZUnnPwx6FCym0wLsk5JZuNx2bPPENzswTNFaQU1RDvt3wT4gWFG" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.min.js" 
    integrity="sha384-j0CNLUeiqtyaRmlzUHCPZ+Gy5fQu0dQ6eZ/xAww941Ai1SxSY+0EQqNXNE6DZiVc" crossorigin="anonymous"></script>
</body>
</html>